const title = (state) => {
  return state.title
}

const version = (state) => {
  return state.version
}

const steps = (state) => {
  return state.steps
}

const step = (state) => {
  return state.step
}

const phases = (state) => {
  return state.phases
}

const phase = (state) => {
  return state.phase
}

const stages = (state) => {
  return state.stages
}

const stage = (state) => {
  return state.stage
}

const dictionary = (state) => {
  return state.dictionary
}

export {
  title,
  version,
  steps,
  step,
  phases,
  phase,
  stages,
  stage,
  dictionary
}
